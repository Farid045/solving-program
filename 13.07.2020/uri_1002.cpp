#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    double a,pi,area;

    pi=3.14159;
    cin>>a;
    area=pi*(a*a);
    cout<<"A="<<fixed<<setprecision(4)<<area<<"\n";

    return 0;

}
